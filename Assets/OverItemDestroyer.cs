using UnityEngine; using System.Collections;  public class OverItemDestroyer : MonoBehaviour {      public GameObject MainCamera;     public GameObject carPrefab;      public GameObject coinPrefab;      public GameObject conePrefab;        // Use this for initialization  void Start () {         Vector3 tmp = GameObject.Find("MainCamera").transform.position;         Vector3 itm = GameObject.Find("carPrefab").transform.position;         Vector3 itm2 = GameObject.Find("coinPrefab").transform.position;         Vector3 itm3 = GameObject.Find("conePrefab").transform.position;            }       // Update is called once per frame  void Update () {          //カメラが通り過ぎたアイテムを削除する 		if(coinPrefab.transform.position.z < MainCamera.transform.position.z){             Destroy(coinPrefab.gameObject);         }           } }  